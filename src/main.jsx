import React from 'react'
import ReactDOM from 'react-dom/client'
import WebOnlyPrototype from './payflip-combined.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WebOnlyPrototype />
  </React.StrictMode>,
)
